server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # This is the magic line that fixes the "404 on Refresh" issue
        try_files $uri $uri/ /index.html;
    }
}